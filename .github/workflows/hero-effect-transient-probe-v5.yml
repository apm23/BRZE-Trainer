name: Hero Effect Transient Probe V5 Read Only

on:
  push:
    branches:
      - instant-death-v4-hover-telemetry
    paths:
      - 'HeroEffectTransientProbeV5/**'
      - '.hero-effect-transient-probe-v5-trigger'
      - '.github/workflows/hero-effect-transient-probe-v5.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Verify strict read-only architecture
        shell: pwsh
        run: |
          $src = Get-Content 'HeroEffectTransientProbeV5/ProbeCore.cs' -Raw
          $forbidden = @('WriteProcessMemory','VirtualAllocEx','VirtualProtectEx','CreateRemoteThread','FlushInstructionCache','RVA_TARGET_HELPER','RVA_MAGIC_CREATE')
          foreach ($token in $forbidden) {
            if ($src.Contains($token)) { throw "Forbidden token in V5 read-only probe: $token" }
          }
          if (-not $src.Contains('PROCESS_VM_READ')) { throw 'PROCESS_VM_READ invariant missing' }
          if (-not $src.Contains('PROCESS_QUERY_INFORMATION')) { throw 'PROCESS_QUERY_INFORMATION invariant missing' }
          Write-Host 'Read-only invariant PASS'

      - name: Compile smoke
        run: dotnet build HeroEffectTransientProbeV5/HeroEffectTransientProbeV5.csproj -c Release -p:Platform=x86

      - name: Publish standalone
        run: dotnet publish HeroEffectTransientProbeV5/HeroEffectTransientProbeV5.csproj -c Release -r win-x86 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:DebugType=None -p:DebugSymbols=false -o out/standalone

      - name: Publish small
        run: dotnet publish HeroEffectTransientProbeV5/HeroEffectTransientProbeV5.csproj -c Release -r win-x86 --self-contained false -p:PublishSingleFile=true -p:DebugType=None -p:DebugSymbols=false -o out/small

      - name: Stage outputs
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path artifact | Out-Null
          Copy-Item 'out/standalone/BRZE-Hero-Effect-Transient-Probe-V5.exe' 'artifact/BRZE-Hero-Effect-Transient-Probe-V5-ReadOnly.exe'
          Copy-Item 'out/small/BRZE-Hero-Effect-Transient-Probe-V5.exe' 'artifact/BRZE-Hero-Effect-Transient-Probe-V5-ReadOnly-Small.exe'

      - name: Hash outputs
        shell: pwsh
        run: |
          Get-FileHash artifact/*.exe -Algorithm SHA256 | Format-Table -AutoSize

      - name: Upload probe V5
        uses: actions/upload-artifact@v4
        with:
          name: BRZE-Hero-Effect-Transient-Probe-V5-ReadOnly
          path: artifact/*
          if-no-files-found: error
