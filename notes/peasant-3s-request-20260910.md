Runtime feedback on merged v1: fixed 1s peasant production is too fast (user reached ~800 peasants within minutes and FPS dropped). Requested balance target: fixed 3 seconds per peasant, preserving the proven native scheduler wrapper and local-only population-gate bypass.

Do not change Selection500/headroom160 semantics while applying this balance-only delta.
