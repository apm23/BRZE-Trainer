# F7 automatic F8-completion runtime test

Runtime-test checkpoint for the F7 rewrite at commit 70f551c. F7 no longer enables the failed training-progress hook; it scans local-player buildings with active training state and applies the same completion writes that were runtime-proven through F8 on a selected training building.
